"use strict";
function procesar(g) {
    if (g.lados == 4) {
        let cuadrado = g;
        cuadrado.lado;
    }
    else if (g.lados == 3) {
        let triangulo = g;
        triangulo.altura;
    }
}
